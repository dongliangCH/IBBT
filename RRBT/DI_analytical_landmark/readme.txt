RRBTprune3: stop when finds first solution + remove repeated computation

RRBTprune5: deterministic (pre-computed) vertices，only plot nodes that actually in the tree (exclude pruned nodes) + remove repeated computation